[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "inmotion-sdk"
version = "0.2.4"
description = "An SDK for integrating with inMotion API"
authors = [{ name = "Jason Waring", email = "jason@softwaringsolutions.com" }]
license = { text = "MIT" }
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
          "pycryptodome",
          "python-dotenv",
          "requests",
          "marshmallow",
          "marshmallow_dataclass"]
classifiers = [
          "License :: OSI Approved :: MIT License",
          "Programming Language :: Python :: 3",
          "Programming Language :: Python :: 3 :: Only",
          "Intended Audience :: Developers",
          "Operating System :: OS Independent"]

[project.urls]
Homepage = "https://github.com/jwaring/inmotion-python-sdk"
Repository = "https://github.com/jwaring/inmotion-python-sdk"
Issues = "https://github.com/jwaring/inmotion-python-sdk/issues"
Documentation = "https://inmotion.io/assets/sdk-docs/inmotion.html"

[project.optional-dependencies]
dev = ["pytest", "tox", "pdoc"]

[project.scripts]
inmotion-smoke-test = "scripts.test:main"

[tool.hatch.build]
include = ["inmotion"]
